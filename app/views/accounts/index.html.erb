<h1><%= @title %></h1>
<section class="account-list">
  <% @accounts.each do |account| %>
    <h4><%= link_to account.name, account_path(account.id), method: :get%></h4>
  <% end %>
</section>
<%= button_to "Add New Account", new_account_path, method: :get, class: "btn btn-default" %>
