<h1><%= @title %></h1>
<ul>
    <% @accounts.each do |account| %>
    <li>
      <%= link_to account.name, account_path(account.id)%>
    </li>
    <% end %>
</ul>
<%= button_to "Add New Account", new_account_path, method: :get, class: "btn btn-default" %><br>
